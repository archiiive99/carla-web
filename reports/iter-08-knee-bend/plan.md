# iter-08-knee-bend plan

Split each leg into thigh + shin nested groups. Knee pivot group
inside hip pivot. Knee bend angle = max(0, sin(phase)) × 0.8
(bends during forward half of swing; clamps to zero during stance).

## Acceptance
tsc clean + harness within noise.
