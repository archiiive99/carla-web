# iter-05-revisit-pathB plan — scoped Hosek-Wilkie

True Hosek needs precomputed coefficient tables (iter-05-revisit-pathB-
full). Scoped minimal version: altitude-dependent turbidity + rayleigh
boost near horizon. At sun_alt=60 formula = Path A (no regression).
At sun_alt=0 turbidity +4, rayleigh +0.3 for warm horizon glow.
